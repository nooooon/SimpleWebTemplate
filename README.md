# SimpleWebTemplate
####Required Components

- Node.js
    - <http://nodejs.org/>

#### global install
    $ sudo npm install -g gulp
    $ sudo npm install -g bower
#### local install
    $ npm install & bower install
#### run
    $ gulp