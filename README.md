# SimpleWebTemplate
####Required Components

- Node.js
    - <http://nodejs.org/>

#### global install
    $ sudo npm install -g gulp
#### local install
    $ npm install
#### run
    $ gulp
#### release build
    $ NODE_ENV=release gulp
